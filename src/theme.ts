import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d6ccc2",
      contrastText: "#222222",
    },
    secondary: {
      main: "#E7D8C9",
    },
    text: {
      primary: "#222222",
    },
    background: {
      default: "#edede9",
    },
  },
});

export default theme;